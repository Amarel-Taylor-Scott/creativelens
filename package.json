{
  "name": "creativelens",
  "version": "1.0.0",
  "description": "Creative intelligence platform for marketing teams using AI-generated content",
  "private": true,
  "scripts": {
    "dev": "docker-compose up",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "docker-compose build",
    "migrate": "cd backend && npx knex migrate:latest --knexfile knexfile.ts",
    "seed": "cd backend && npx knex seed:run --knexfile knexfile.ts",
    "test": "cd backend && npm test && cd ../frontend && npm test"
  },
  "keywords": ["creative-intelligence", "ai-content", "marketing", "prompt-generation", "gallery"],
  "author": "Polsia",
  "license": "UNLICENSED"
}
